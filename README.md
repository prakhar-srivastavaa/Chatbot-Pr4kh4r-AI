
  # chatbot (Community)

  This is a code bundle for chatbot (Community). The original project is available at https://www.figma.com/design/g2iH8eRapGJBfT0pS6zNni/chatbot--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  